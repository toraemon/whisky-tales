# 📝 WhiskyTales 開発ログ：2025年5月19日（月）

## ✅ 今日やったこと（成功した内容）

### 🛠 開発環境構築
- `brew install --cask docker` で Docker Desktop を導入
- シェル補完ファイルの競合（_docker, docker.fishなど）を手動削除し、インストール成功
- Docker 起動確認：`docker ps` OK

### 📂 ディレクトリ構成設計
- `usecases/`：ユースケース・トランザクション制御層
- `logics/`：業務ロジック層（再利用関数・純粋なドメイン処理）
- その他：`controllers/`, `routes/`, `repositories/`, `schemas/`, `middlewares/`, `config/`, `utils/` 構成済み
- 設計意図は `docs/backend-architecture/folder-structure.md` に保存済み（テンプレで作成）

### ⚙️ プロジェクト起動と動作確認
- `ts-node-dev` + `typescript` + `express` 環境構築
- `app.ts` / `index.ts` 分離構成でサーバー起動成功
- `prisma`, `@prisma/client` インストール済み
- `.env` に `DATABASE_URL` を設定し、Prisma がDBに接続成功
- `npx prisma migrate dev` にて `whiskies` テーブル作成
- `yarn dev` → `GET /whiskies` にて `"[]"` のレスポンス確認
- `Prisma Studio` でGUIからデータ追加
- 「山崎」を追加 → `curl http://localhost:3000/whiskies` で取得成功 🎉

---

## ⏭ 次にやるべきこと（TODO）

### ① 🧾 API設計
- `POST /whiskies` をはじめとしたCRUDルートの設計
- Zod等によるバリデーション導入
- API設計ガイドラインを `docs/backend-architecture/api-guideline.md` に記載予定

### ② 🛠 Makefile の作成
- よく使うコマンドをMakefileに集約

例：
```make
dev:
	yarn dev

migrate:
	yarn prisma migrate dev

generate:
	yarn prisma generate

studio:
	yarn prisma studio
```

### ③ 💻 DBクライアントツールの導入
- GUIツール候補：
  - Postico（軽量・Mac）
  - TablePlus（多機能・UI綺麗）
  - pgAdmin（Postgres公式）
- `.env` の接続情報を元にクライアントに接続

---

## 🐾 次回作業の起点

- Makefile作成
- `POST /whiskies` 実装＆バリデーション
- クライアントツール導入
